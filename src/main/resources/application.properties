#In a production environment this would be changed to avoid keeping user details within the code
#itself. Most likely using a module like Spring Vault or through AWS KMS integration. Through
#a dev environment you might use environment variables more as well than directly like below.

spring.jpa.hibernate.ddl-auto=none
spring.datasource.url=jdbc:mysql://localhost:3306/metrics?serverTimezone=GMT
spring.datasource.username=root
spring.datasource.password=root